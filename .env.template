# ExactCue needs no application secrets.
# Netlify injects the site/token context used by @netlify/blobs into Functions.
# `pnpm dev` and `pnpm preview` use the same order service over labeled local memory.
