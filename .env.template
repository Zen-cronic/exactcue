# Handsfree environment template. Copy to .env.local for local dev.
# The iteration-1 client build needs no secrets. Netlify Blobs (iteration 2) is
# auto-configured on Netlify and under `netlify dev`.
